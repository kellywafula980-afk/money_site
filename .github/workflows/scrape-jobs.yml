name: Daily Job Scraper

on:
  schedule:
    - cron: '0 12 * * *'
  workflow_dispatch:

jobs:
  scrape-jobs:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger job scraper on Render
        run: |
          echo "🚀 Starting job scraper..."
          curl -s "https://globalgigs-0096.onrender.com/run-production-sync/?key=candy2026"
          echo ""
          echo "✅ Job scraper completed!"
      
      - name: Ping Google about sitemap update
        run: |
          echo "📢 Notifying Google about sitemap update..."
          curl -s "https://www.google.com/ping?sitemap=https://globalgigs-0096.onrender.com/sitemap.xml"
          echo "✅ Google notified!"
